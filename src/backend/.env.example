# =================================================
# Know Your Potential - Environment Configuration
# =================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git (contains secrets!)

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=5000
NODE_ENV=development

# ============================================
# DATABASE CONFIGURATION
# ============================================
# DEVELOPMENT (Docker): Use local MongoDB container
# When using docker-compose, this is set automatically in docker-compose.yml
# For local development without Docker, use:
MONGODB_URI=mongodb://localhost:27017/knowyourpotential

# PRODUCTION (Atlas): Use MongoDB Atlas cloud database
# Uncomment and fill in your Atlas connection string for production:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/knowyourpotential?retryWrites=true&w=majority

# ============================================
# AUTHENTICATION
# ============================================
# JWT Secret Key (change this to a random string in production!)
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
JWT_EXPIRE=7d

# ============================================
# CORS CONFIGURATION
# ============================================
# Frontend URL (change in production to your deployed frontend URL)
CORS_ORIGIN=http://localhost:3000
